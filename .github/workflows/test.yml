name: test

on: push

jobs:
  call:
    uses: ./.github/workflows/callable.yml

  dependent:
    if: always()
    needs: call
    runs-on: ubuntu-latest
    env:
      CALL_JSON: ${{ toJson(needs.call) }}
    steps:
      - run: |
          echo "${CALL_JSON}"
